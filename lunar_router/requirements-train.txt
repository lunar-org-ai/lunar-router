# ============================================================================
# Lunar Router — Training Dependencies
# ============================================================================
# Install with:  pip install -r requirements-train.txt
# Or:            make install-train
#
# Requires: PyTorch with CUDA (install separately first)
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# ============================================================================

# --- Core ML ---
transformers>=4.45.0
accelerate>=0.34.0
huggingface-hub>=0.34.0
tokenizers>=0.20.0
safetensors>=0.4.0
datasets>=2.14.0

# --- Fine-tuning ---
unsloth
trl>=0.7.0
peft>=0.6.0
bitsandbytes>=0.41.0

# --- GGUF Export ---
gguf>=0.6.0

# --- Utilities ---
sentencepiece>=0.2.0
protobuf>=3.20.0
scipy>=1.10.0
