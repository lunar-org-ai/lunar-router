# Node
node_modules/
ui/dist/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific
*.log
.env.local
.env.*.local

# Secrets (API keys stored at runtime)
.opentracy/
secrets.json

# Harness memory (runtime-generated)
opentracy/harness/memory/*.md

# Go build artifacts
go/bin/

# ONNX models
*.onnx

# Weights (download separately)
weights/
!go/internal/weights/
*.npz
*.zip

# Keep dist for packaged weights
!dist/weights-*.zip

# Runtime data & caches
data/
.vite/
outputs/
unsloth_compiled_cache/

# Per-platform Go binary + onnxruntime lib + ONNX MiniLM model — CI downloads
# these fresh during each release job and sets up package_data; local-only.
opentracy/_bin/
opentracy/_onnx/
dist_multi/

# Claude Code local state (memory, tasks, session notes)
.claude/

# Training artifacts (accumulate when distillation jobs run locally)
wandb/
mlruns/
checkpoints/
checkpoint-*/
*.safetensors
*.gguf
*.pth
adapter_model.*
tokenizer.model
training_state.json

# Notebook runtime artifacts — generated by running cells, not source.
# `serve.py` is written by the FastAPI cell in nb06; `ticket-classifier-v1/`
# is the path student.save() produces in nb05/nb06; `_bundled_weights/*/onnx/`
# is the MiniLM ONNX that the release CI stages into `_onnx/` during wheel build.
notebooks/serve.py
notebooks/ticket-classifier-v1/
notebooks/demo-student/
opentracy/_bundled_weights/weights-mmlu-v1/onnx/
