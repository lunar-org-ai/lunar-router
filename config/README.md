# config/

Top-level configuration. Source of truth for what the harness can and can't do.

- `claude_code.yaml` — allowlist of paths Claude Code can mutate / read / append to.
- `deployment.yaml` — deployment-level config (env, model providers, limits).
